export function funcionlineal(x : number[], a : number, b : number){
    y : Number[];
    var numDatos;
    numDatos = x.length;

    for (var i = 0; i<numDatos; i++){
        y = a * x + b;
    }
    
}